<Page
    x:Class="electrifier.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:electrifier.Helpers"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    xmlns:xaml1="using:electrifier.ViewModels"
    mc:Ignorable="d">

    <!-- x:Class="electrifier.Views.SettingsPage"-->

    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0">
            <!-- Personalization -->
            <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                <!-- Left Column: Settings -> Theme -->
                <TextBlock x:Uid="Settings_Theme" />
                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <RadioButton
                        x:Uid="Settings_Theme_Light"
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}"
                        FontSize="15"
                        GroupName="AppTheme">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Light</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton
                        x:Uid="Settings_Theme_Dark"
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}"
                        FontSize="15"
                        GroupName="AppTheme">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Dark</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton
                        x:Uid="Settings_Theme_Default"
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default, Mode=OneWay}"
                        FontSize="15"
                        GroupName="AppTheme">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Default</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                </StackPanel>

                <TextBlock x:Uid="Settings_Language" Margin="{StaticResource LargeTopMargin}" />
                <!-- Settings_Theme.Text -->
                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <RadioButtons>
                        <RadioButton x:Uid="Settings_Language_English" />
                        <RadioButton x:Uid="Settings_Language_German" />
                    </RadioButtons>

                    <!--<RadioButton
                        x:Uid="Settings_Language_English"
                        Command="{x:Bind ViewModel.SwitchLanguageCommand}"

                        
                        FontSize="15"
                        GroupName="AppLanguage">

                        -->
                    <!--IsChecked="{x:Bind ViewModel.LanguageId, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=English, Mode=OneWay}"-->
                    <!--
                        
                        <RadioButton.CommandParameter>
                            <xaml1:ElementLanguage>English</xaml1:ElementLanguage>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton
                        x:Uid="Settings_Language_German"
                        Command="{x:Bind ViewModel.SwitchLanguageCommand}"
                        FontSize="15"
                        GroupName="AppLanguage">
                        <RadioButton.CommandParameter>
                            <xaml1:ElementLanguage>German</xaml1:ElementLanguage>
                        </RadioButton.CommandParameter>
                    </RadioButton>-->

                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- About -->
        <StackPanel Grid.Column="1">
            <!-- -->
            <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <StackPanel Margin="{StaticResource XSmallTopMargin}">
                <TextBlock Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" Style="{ThemeResource BodyTextBlockStyle}" />
                <TextBlock
                    x:Uid="Settings_AboutDescription"
                    Margin="{StaticResource XSmallTopMargin}"
                    Style="{ThemeResource BodyTextBlockStyle}" />
                <HyperlinkButton x:Uid="SettingsPage_PrivacyTermsLink" Margin="{StaticResource LargeTopMargin}"/>
            </StackPanel>

            <TextBlock x:Uid="Settings_Credits" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="{StaticResource LargeTopMargin}"/>
        </StackPanel>
    </Grid>
</Page>
